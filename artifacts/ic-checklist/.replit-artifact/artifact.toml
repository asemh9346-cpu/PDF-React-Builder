kind = "web"
previewPath = "/"
title = "IC Weekly Round Checklist"
version = "1.0.0"
id = "artifacts/ic-checklist"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20691

[services.development]
run = "pnpm --filter @workspace/ic-checklist run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ic-checklist", "run", "build" ]
publicDir = "artifacts/ic-checklist/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20691"
BASE_PATH = "/"
